# Basic Distinguisher

Check whether the spec obviously contradicts the task or misses something
important.

Return only major issues. If the spec looks mostly reasonable, return an empty
issue list.

Prefer generator-routed issues. Ask the user only when the task gives no clue.
